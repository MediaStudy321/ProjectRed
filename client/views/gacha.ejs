<html>

<head>
  <meta charset="UTF-8">
  <title>Rolls</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="/gamestyle.css" />
  <link rel="preconnect" href="https://fonts.gstatic.com">
  <link href="https://fonts.googleapis.com/css2?family=Staatliches&display=swap" rel="stylesheet">
  <script src="https://code.jquery.com/jquery-3.6.0.min.js"
		integrity="sha256-/xUj+3OJU5yExlq6GSYGSHk7tPXikynS7ogEvDej/m4="
		crossorigin="anonymous"></script>
</head>
<body>

  <div id="header">
    <div id="split">
     <div id = "small"><div id = "center2"><h1 style="float:left; padding-top:12px; padding-left:15px;">Profile Name: no name</h1></div> </div>
      <div id = "small"><div id = "center2"><h1 style="float:left; padding-top:12px;">Energy: 10/10</h1></div> </div>
      <div id = "small"><div id = "center2"><h1 style="float:left; padding-top:12px; padding-right:15px;">Gold: broke</h1></div> </div>
    </div>
  </div>
  <div id="divider"></div>
  <div id="rolls">
    <button onclick = "abyss()" align="center">Abyssal Roll</button>
  </div>
  <div id="divider"></div>
  <script src="/js/gacha.js"></script>
  <script>
    let int = 0;

    function getRandomInt(min, max) {
      min = Math.ceil(min);
      max = Math.floor(max);
     return Math.floor(Math.random() * (max - min) + min);
    }

    function roll(){
      int = getRandomInt(1,heroSet.length);
      return heroSet[int];
    }

    function abyss() {
      console.log("yes");
      var character = roll();
      console.log(character);
      while((player.characters).includes(character)){
        character = roll();
        console.log(character);

      }
      $.ajax({
        url: 'roll/',
        type: 'GET',
        success: (response)=>{
            console.log(response);
            $('#rolls').html(response);
        },
        error: (e)=>{
            console.log(e);
        }
    });
    }
    </script>
</body>

</html>